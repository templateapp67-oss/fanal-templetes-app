import React, { useState } from 'react';
import { Bell, Check, ChevronDown, Download, FileText, HelpCircle, IndianRupee, Medal, MessageCircle, Send, Trophy, Wallet } from 'lucide-react';
import { PartnerToast } from './PartnerToast';

const money = (n: number) => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(n);
const Card = ({ label, value, hint }: { label: string; value: string; hint?: string }) => <div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-2xl font-black text-slate-950">{value}</p><p className="mt-1 text-sm font-bold text-slate-600">{label}</p>{hint ? <p className="mt-1 text-xs text-slate-500">{hint}</p> : null}</div>;
const Button = ({ children, onClick, secondary = false, type = 'button' }: { children: React.ReactNode; onClick?: () => void; secondary?: boolean; type?: 'button' | 'submit' }) => <button type={type} onClick={onClick} className={`inline-flex min-h-11 items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-bold transition-opacity hover:opacity-85 ${secondary ? 'bg-slate-100 text-slate-800' : 'bg-slate-900 text-white'}`}>{children}</button>;
const csvDownload = (name: string, rows: string[][]) => { const blob = new Blob([rows.map(row => row.map(v => `"${v.replaceAll('"', '""')}"`).join(',')).join('\n')], { type: 'text/csv' }); const a = document.createElement('a'); a.href = URL.createObjectURL(blob); a.download = name; a.click(); URL.revokeObjectURL(a.href); };

export function PartnerEarningsModule() {
  const transactions = [['Nexora Plus referral', '12 Sep 2026', 'Completed', '₹1,250'], ['Growth bonus', '01 Sep 2026', 'Completed', '₹500'], ['Nexora Starter referral', '29 Aug 2026', 'Pending', '₹750']];
  const [notice, setNotice] = useState('');
  return <div className="space-y-5"><PartnerToast message={notice} noticeId={notice.length} /><section className="rounded-3xl bg-gradient-to-br from-slate-950 to-[#8b0a46] p-6 text-white sm:p-8"><Wallet className="h-8 w-8 text-pink-200" /><h2 className="mt-4 text-2xl font-black">Your earnings at a glance</h2><p className="mt-1 text-sm text-slate-200">Track referral income, pending commissions and payment history.</p></section><section className="grid gap-4 sm:grid-cols-3"><Card label="Lifetime earnings" value={money(18450)} hint="Across 28 successful referrals" /><Card label="Pending earnings" value={money(2250)} hint="Released after verification" /><Card label="Last month" value={money(4750)} hint="+18% from August" /></section><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="font-black">Monthly revenue</h2><p className="text-sm text-slate-500">Last six months</p></div><div className="flex gap-2"><Button secondary onClick={() => csvDownload('nexora-earnings.csv', [['Description', 'Date', 'Status', 'Amount'], ...transactions])}><Download className="h-4 w-4" />CSV</Button><Button secondary onClick={() => window.print()}><FileText className="h-4 w-4" />PDF</Button></div></div><div className="mt-7 flex h-44 items-end gap-3">{[42, 55, 48, 70, 62, 86].map((height, i) => <div key={i} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-xl bg-[#c20e5a]" style={{ height: `${height}%` }} /><span className="text-xs font-bold text-slate-500">{['Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep'][i]}</span></div>)}</div></section><section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><h2 className="font-black">Transaction history</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[560px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{['Description', 'Date', 'Status', 'Amount'].map(x => <th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody>{transactions.map(row => <tr key={row[0]} className="border-t border-slate-100">{row.map((x, i) => <td key={i} className="px-5 py-4 font-medium">{i === 2 ? <span className={`rounded-full px-2 py-1 text-xs ${x === 'Completed' ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700'}`}>{x}</span> : x}</td>)}</tr>)}</tbody></table></div></section></div>;
}

export function PartnerWithdrawalsModule() {
  const [open, setOpen] = useState(false), [method, setMethod] = useState('UPI'), [amount, setAmount] = useState(''), [notice, setNotice] = useState('');
  const submit = () => { const value = Number(amount); if (!value || value < 500) return setNotice('Minimum withdrawal amount is ₹500.'); if (value > 2250) return setNotice('Amount exceeds your available balance.'); setOpen(false); setAmount(''); setNotice(`Withdrawal request for ${money(value)} submitted via ${method}.`); };
  return <div className="space-y-5"><PartnerToast message={notice} noticeId={notice.length} /><section className="rounded-3xl bg-slate-950 p-6 text-white"><p className="text-sm font-bold text-pink-200">Available to withdraw</p><p className="mt-2 text-4xl font-black">{money(2250)}</p><p className="mt-2 text-sm text-slate-300">Minimum withdrawal: ₹500</p><Button onClick={() => setOpen(true)}><IndianRupee className="h-4 w-4" />Request withdrawal</Button></section>{open ? <section className="rounded-3xl border border-pink-200 bg-white p-6 shadow-xl"><div className="flex justify-between"><h2 className="text-lg font-black">Request withdrawal</h2><button onClick={() => setOpen(false)} className="text-slate-500">Close</button></div><div className="mt-4 grid gap-3 sm:grid-cols-3">{['UPI', 'Bank Transfer', 'PayPal'].map(x => <button key={x} onClick={() => setMethod(x)} className={`rounded-xl border p-3 text-sm font-bold ${method === x ? 'border-[#c20e5a] bg-pink-50 text-[#a40c4c]' : 'border-slate-200'}`}>{x}</button>)}</div><label className="mt-4 block text-sm font-bold">Amount <input value={amount} onChange={e => setAmount(e.target.value.replace(/[^0-9]/g, ''))} placeholder="500" className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3" /></label><div className="mt-4 flex gap-3"><Button onClick={submit}>Submit request</Button><Button secondary onClick={() => setOpen(false)}>Cancel</Button></div></section> : null}<section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-black">Payout tracker</h2><ol className="mt-5 grid gap-3 sm:grid-cols-3">{['Request submitted', 'Under review', 'Paid to your method'].map((x, i) => <li key={x} className="rounded-2xl bg-slate-50 p-4"><span className={`flex h-7 w-7 items-center justify-center rounded-full text-xs font-black ${i === 0 ? 'bg-emerald-600 text-white' : 'bg-slate-200 text-slate-500'}`}>{i === 0 ? <Check className="h-4 w-4" /> : i + 1}</span><p className="mt-3 text-sm font-bold">{x}</p></li>)}</ol></section><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-black">Completed payouts</h2><p className="mt-3 text-sm text-slate-600">₹4,750 · UPI · Paid on 05 Sep 2026</p></section></div>;
}

const assets = [{ category: 'Banners', name: 'Nexora growth banner', type: 'PNG' }, { category: 'Email Templates', name: 'Partner introduction email', type: 'TXT' }, { category: 'Social Media Graphics', name: 'Referral story graphic', type: 'PNG' }, { category: 'Video Demos', name: 'Nexora product walkthrough', type: 'MP4' }];
export function PartnerMarketingMaterialsModule() { const [filter, setFilter] = useState('All'), [notice, setNotice] = useState(''); const shown = filter === 'All' ? assets : assets.filter(x => x.category === filter); return <div className="space-y-5"><PartnerToast message={notice} noticeId={notice.length} /><section className="rounded-3xl bg-gradient-to-br from-[#8b0a46] to-[#ed176f] p-6 text-white"><h2 className="text-2xl font-black">Marketing materials</h2><p className="mt-2 text-sm text-pink-100">Ready-to-share assets and copy for your referral campaigns.</p></section><div className="flex flex-wrap gap-2">{['All', ...new Set(assets.map(x => x.category))].map(x => <button key={x} onClick={() => setFilter(x)} className={`rounded-full px-4 py-2 text-sm font-bold ${filter === x ? 'bg-slate-900 text-white' : 'bg-white text-slate-600'}`}>{x}</button>)}</div><section className="grid gap-4 md:grid-cols-2">{shown.map(a => <article key={a.name} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex h-24 items-center justify-center rounded-2xl bg-pink-50 text-[#c20e5a]"><FileText className="h-9 w-9" /></div><p className="mt-4 text-xs font-bold uppercase text-slate-500">{a.category} · {a.type}</p><h3 className="mt-1 font-black">{a.name}</h3><Button secondary onClick={() => { csvDownload(a.name.replaceAll(' ', '-').toLowerCase() + '.csv', [['Nexora marketing asset'], [a.name]]); setNotice(`${a.name} downloaded.`); }}><Download className="h-4 w-4" />Download</Button></article>)}</section><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-black">Quick-copy caption</h2><p className="mt-3 rounded-2xl bg-slate-50 p-4 text-sm text-slate-700">Build a stronger online presence with Nexora. Join using my link and get started today.</p><Button secondary onClick={() => { navigator.clipboard?.writeText('Build a stronger online presence with Nexora. Join using my link and get started today.'); setNotice('Caption copied to clipboard.'); }}>Copy caption</Button></section></div>; }

export function PartnerLevelsModule() { const levels = [['Bronze', '10%', '0'], ['Silver', '12%', '10'], ['Gold', '15%', '25'], ['Platinum', '18%', '50']]; return <div className="space-y-5"><section className="rounded-3xl bg-slate-950 p-6 text-white"><Medal className="h-8 w-8 text-amber-300" /><p className="mt-4 text-sm font-bold text-amber-200">Current level</p><h2 className="text-3xl font-black">Silver Partner</h2><p className="mt-2 text-sm text-slate-300">18 of 25 paid referrals — 7 more to unlock Gold.</p><div className="mt-4 h-3 overflow-hidden rounded-full bg-white/20"><div className="h-full w-[72%] rounded-full bg-amber-300" /></div></section><section className="grid gap-4 lg:grid-cols-4">{levels.map(([level, commission, requirement]) => <article key={level} className={`rounded-3xl border bg-white p-5 shadow-sm ${level === 'Silver' ? 'border-[#c20e5a] ring-1 ring-[#c20e5a]' : 'border-slate-200'}`}><p className="font-black">{level}</p><p className="mt-3 text-2xl font-black text-[#c20e5a]">{commission}</p><p className="text-sm text-slate-500">Commission rate</p><p className="mt-4 text-xs font-bold text-slate-600">{requirement} paid referrals required</p><p className="mt-2 text-xs text-slate-500">Priority support {level === 'Gold' || level === 'Platinum' ? 'included' : 'available'}</p></article>)}</section></div>; }

const leaderboard = [['Aarav Mehta', '42', '₹28,500'], ['Sana Kapoor', '37', '₹24,600'], ['You', '28', '₹18,450'], ['Rohan Shah', '25', '₹16,900']];
export function PartnerLeaderboardsModule() { const [period, setPeriod] = useState('Monthly'); return <div className="space-y-5"><section className="flex flex-wrap justify-between gap-3 rounded-3xl bg-slate-950 p-6 text-white"><div><Trophy className="h-8 w-8 text-amber-300" /><h2 className="mt-3 text-2xl font-black">Partner leaderboard</h2><p className="text-sm text-slate-300">You are ranked #3 this month.</p></div><select value={period} onChange={e => setPeriod(e.target.value)} className="h-11 rounded-xl bg-white px-3 text-sm font-bold text-slate-900"><option>Weekly</option><option>Monthly</option><option>Yearly</option></select></section><section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="p-5"><h2 className="font-black">{period} rankings</h2></div>{leaderboard.map((x, i) => <div key={x[0]} className={`flex items-center gap-4 border-t border-slate-100 px-5 py-4 ${x[0] === 'You' ? 'bg-pink-50' : ''}`}><span className={`flex h-8 w-8 items-center justify-center rounded-full font-black ${i < 3 ? 'bg-amber-100 text-amber-800' : 'bg-slate-100 text-slate-500'}`}>{i + 1}</span><div className="min-w-0 flex-1"><p className="font-bold">{x[0]}</p><p className="text-xs text-slate-500">{x[1]} referrals</p></div><p className="font-black">{x[2]}</p></div>)}</section></div>; }

export function PartnerNotificationsModule() { const initial = [{ id: 1, type: 'Referrals', title: 'A new referral signed up', body: 'Priya Sharma joined using your link.', unread: true }, { id: 2, type: 'Payouts', title: 'Payout processed', body: 'Your ₹4,750 payout is complete.', unread: true }, { id: 3, type: 'System', title: 'New marketing material', body: 'September social assets are now available.', unread: false }]; const [items, setItems] = useState(initial), [tab, setTab] = useState('All'), [selected, setSelected] = useState<typeof initial[0] | null>(null); const shown = items.filter(x => tab === 'All' || x.type === tab); return <div className="space-y-5"><section className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-2xl font-black">Notifications</h2><p className="text-sm text-slate-500">{items.filter(x => x.unread).length} unread updates</p></div><Button secondary onClick={() => setItems(x => x.map(n => ({ ...n, unread: false })))}><Check className="h-4 w-4" />Mark all as read</Button></section><div className="flex flex-wrap gap-2">{['All', 'System', 'Payouts', 'Referrals'].map(x => <button key={x} onClick={() => setTab(x)} className={`rounded-full px-4 py-2 text-sm font-bold ${tab === x ? 'bg-slate-900 text-white' : 'bg-white text-slate-600'}`}>{x}</button>)}</div><section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm">{shown.map(n => <button key={n.id} onClick={() => { setItems(x => x.map(v => v.id === n.id ? { ...v, unread: false } : v)); setSelected(n); }} className="flex w-full items-start gap-3 border-b border-slate-100 p-5 text-left hover:bg-slate-50"><Bell className="mt-1 h-4 w-4 text-[#c20e5a]" /><div className="flex-1"><p className="font-bold">{n.title} {n.unread ? <span className="ml-1 inline-block h-2 w-2 rounded-full bg-[#c20e5a]" /> : null}</p><p className="mt-1 text-sm text-slate-600">{n.body}</p></div></button>)}</section>{selected ? <section className="rounded-3xl border border-pink-200 bg-pink-50 p-5"><h3 className="font-black">{selected.title}</h3><p className="mt-2 text-sm">{selected.body}</p><Button secondary onClick={() => setSelected(null)}>Close detail</Button></section> : null}<section className="rounded-3xl border border-slate-200 bg-white p-5"><h3 className="font-black">Preferences</h3><label className="mt-3 flex gap-2 text-sm"><input type="checkbox" defaultChecked /> Email notifications</label><label className="mt-2 flex gap-2 text-sm"><input type="checkbox" defaultChecked /> In-app notifications</label></section></div>; }

export function PartnerSupportModule() { const [query, setQuery] = useState(''), [open, setOpen] = useState<number | null>(null), [notice, setNotice] = useState(''); const faq = [['When are earnings available?', 'Earnings are released after the associated referral has completed the verification period.'], ['How do I request a payout?', 'Open Withdrawals, choose your method, and request at least ₹500.'], ['How is my referral tracked?', 'Each new user who joins using your personal link or code is attributed to you.']].filter(x => x.join(' ').toLowerCase().includes(query.toLowerCase())); return <div className="space-y-5"><PartnerToast message={notice} noticeId={notice.length} /><section className="rounded-3xl bg-slate-950 p-6 text-white"><HelpCircle className="h-8 w-8 text-pink-200" /><h2 className="mt-3 text-2xl font-black">How can we help?</h2><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search FAQs" className="mt-4 w-full rounded-xl bg-white px-4 py-3 text-sm text-slate-900" /></section><section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm">{faq.map(([q, a], i) => <div key={q} className="border-b border-slate-100"><button onClick={() => setOpen(open === i ? null : i)} className="flex w-full items-center justify-between p-5 text-left font-bold">{q}<ChevronDown className={`h-4 w-4 transition-transform ${open === i ? 'rotate-180' : ''}`} /></button>{open === i ? <p className="px-5 pb-5 text-sm text-slate-600">{a}</p> : null}</div>)}</section><form onSubmit={e => { e.preventDefault(); setNotice('Your support ticket has been submitted. We will update you here.'); (e.currentTarget as HTMLFormElement).reset(); }} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-black">Submit a support ticket</h2><input required placeholder="Subject" className="mt-4 w-full rounded-xl border border-slate-200 px-3 py-3 text-sm" /><textarea required placeholder="Describe what you need help with" className="mt-3 min-h-28 w-full rounded-xl border border-slate-200 px-3 py-3 text-sm" /><input type="file" className="mt-3 block text-sm" /><div className="mt-4 flex flex-wrap gap-3"><Button type="submit"><Send className="h-4 w-4" />Submit ticket</Button><a href="https://wa.me/" target="_blank" rel="noreferrer" className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-bold text-white"><MessageCircle className="h-4 w-4" />WhatsApp</a></div></form><section className="rounded-3xl border border-slate-200 bg-white p-5"><h2 className="font-black">Ticket history</h2><p className="mt-3 text-sm font-semibold">#NX-1042 · Referral link question <span className="ml-2 rounded-full bg-amber-50 px-2 py-1 text-xs text-amber-700">In progress</span></p></section></div>; }
