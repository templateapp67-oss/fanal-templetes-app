# ============================================================================
# SHARED DEVELOPMENT / PREVIEW CREDENTIALS  —  TEST MODE ONLY
# ----------------------------------------------------------------------------
# This file IS committed on purpose so the preview/sandbox and CI always have
# working Razorpay TEST keys (test mode can never move real money).
#
# Precedence: real environment variables  >  .env  >  .env.development
# So anything you set on Vercel / Cloud Run / in .env automatically wins.
#
# NEVER put rzp_live_* keys, the Supabase service-role key, or any other real
# secret in this file — those belong in .env (git-ignored) or in your hosting
# provider's environment settings.
# ============================================================================

RAZORPAY_KEY_ID="rzp_test_TIzKly1Z2NMnum"
RAZORPAY_KEY_SECRET="9SehLfvRW6eVtHXtFXzL2Ovm"

# --- Razorpay webhook ------------------------------------------------------
# The webhook secret is a value YOU choose: paste the SAME string here and in
# the Razorpay dashboard (Settings -> Webhooks -> Add New Webhook -> Secret).
# Webhook URL: https://fanal-templetes-app.vercel.app/api/payments/razorpay/webhook
RAZORPAY_WEBHOOK_SECRET="C9EWXhp3cHnow4oUGIzeCTIXzbmswV3Y"
